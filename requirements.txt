# Core data processing
pandas>=2.0.0

# PDF extraction
camelot-py>=0.11.0
PyPDF2>=3.0.0

# HTTP requests
requests>=2.31.0

# Environment variables
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0

# Fuzzy string matching for bilingual search
thefuzz>=0.22.0
python-Levenshtein>=0.25.0

# Concurrent processing (built-in, but listed for clarity)
# concurrent.futures - part of stdlib

# MCP Server SDK
mcp>=1.0.0

# DuckDB for local storage and similarity search (FTS + VSS)
duckdb>=1.1.0

# Multilingual sentence embeddings for semantic similarity (VSS pipeline)
sentence-transformers>=3.0.0

# Retry logic for API rate limiting
tenacity>=8.0.0

# For LLM API integration (optional - can use environment variables)
# openai>=1.0.0
# anthropic>=0.20.0
